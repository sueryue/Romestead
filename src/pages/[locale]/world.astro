---
import Base from '../../layouts/Base.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { getUi } from '../../i18n/ui';
import { LOCALES, link, pick, media, type Locale } from '../../i18n/utils';
import { WORLD } from '../../content/world';
import { SITE } from '../../consts';

export function getStaticPaths() {
  return LOCALES.map((locale) => ({ params: { locale } }));
}

const { locale } = Astro.params as { locale: Locale };
const ui = getUi(locale);
const path = '/world';
const intro = pick(WORLD.intro, locale);
---

<Base
  locale={locale}
  title={`${ui['nav.world']} — ${SITE.name}`}
  description={intro}
  path={path}
>
  <Header locale={locale} path={path} />
  <main class="mx-auto max-w-4xl px-4 py-12">
    <h1 class="font-display text-3xl font-black text-roman-700">{ui['nav.world']}</h1>
    <p class="mt-4 text-lg leading-relaxed text-ink-700">{intro}</p>

    <section class="mt-10">
      <h2 class="font-display text-2xl font-bold text-roman-700" data-i18n="world.biomes">{ui['world.biomes']}</h2>
      <div class="mt-4 grid gap-6 sm:grid-cols-2">
        {WORLD.biomes.map((b) => (
          <div class="reveal-ready overflow-hidden rounded-lg border border-gold-600/30 bg-parchment-50 shadow-sm">
            {b.image && (
              <img src={media(b.image)} alt={pick(b.name, locale)} loading="lazy" class="h-48 w-full object-cover" />
            )}
            <div class="p-5">
              <h3 class="font-display text-lg font-bold text-ink-800">{pick(b.name, locale)}</h3>
              <p class="mt-2 text-sm text-ink-700/80">{pick(b.desc, locale)}</p>
              {b.threats && (
                <p class="mt-3 rounded bg-roman-600/10 px-3 py-2 text-xs font-semibold text-roman-600">
                  ⚔ {pick(b.threats, locale)}
                </p>
              )}
            </div>
          </div>
        ))}
      </div>
    </section>
  </main>
  <Footer locale={locale} />
</Base>
